triangulation_done = False
polygon = Polygon([# coords])
triangulated_polygram = Polygram([])

while not triangulation_done:
    if polygon == Triangle:
        triangulation_done = True
    if not triangulation_done:
        ear_coords = polygon.find_ears()   # find_ears() means that we use code from previous code block which execute O(kn) time
        diagonal = (# create a diagonal using ear_coords)
        polygon = Polygon([# coords, but without verticle which we have used like ear])
        triangulated_polygram = triangulated_polygram + diagonal

return triangulated_polygram
